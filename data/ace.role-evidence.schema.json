{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ACE Role to Evidence Mapping (Dormant)",
  "type": "object",
  "required": ["engine", "version", "governance", "roles"],
  "properties": {
    "engine": { "type": "string", "const": "ACE" },
    "version": { "type": "string" },
    "governance": {
      "type": "object",
      "required": ["mode", "exposed", "note"],
      "properties": {
        "mode": { "type": "string", "const": "structure-only" },
        "exposed": { "type": "boolean", "const": false },
        "note": { "type": "string" }
      },
      "additionalProperties": false
    },
    "roles": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "label", "status", "evidence_domains"],
        "properties": {
          "id": { "type": "string", "pattern": "^[a-z0-9\\-]+$" },
          "label": { "type": "string" },
          "status": { "type": "string", "const": "dormant" },
          "evidence_domains": {
            "type": "array",
            "items": { "type": "string" }
          }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
